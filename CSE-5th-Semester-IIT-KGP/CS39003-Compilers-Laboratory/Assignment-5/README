To generate the object files and compiled binaries run:
    `make`
To clean the object files and compiled binaries run:
    `make clean`
To run the tests run the following command:
    `make test`
To clean the .txt files and compiled binaries:
    `make test_clean`

